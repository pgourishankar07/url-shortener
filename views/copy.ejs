<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <title>Bit.url</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="icon" type="image/png" href="/favicon_io/favicon.ico" />
    <style>
      .snippet-box {
        display: flex;
        align-items: center;
        background-color: #f8f9fa;
        border: 1px solid #ddd;
        padding: 10px;
        border-radius: 5px;
        max-width: 500px;
      }
      .snippet-text {
        flex-grow: 1;
        margin-right: 10px;
        font-family: monospace;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      .copy-btn {
        cursor: pointer;
      }
    </style>
  </head>
  <body class="p-3 m-0 border-0 bd-example m-0 border-0">
    <div class="container text-center">
      <h1><span class="text-primary">Bit.url</span> URL Shortener</h1>
    </div>

    <h2>Your Shortened URL:</h2>

    <div class="snippet-box">
      <span class="snippet-text" id="short-url">
        http://localhost:3000/url/<%= url %>
      </span>
      <button
        class="btn btn-primary btn-sm copy-btn"
        onclick="copyToClipboard()"
      >
        Copy
      </button>
    </div>

    <div class="mb-3">
      <div id="emailHelp" class="form-text">Please save this URL to use.</div>
    </div>
    <a href="/"><button class="btn btn-primary">Back</button></a>

    <script>
      function copyToClipboard() {
        const urlText = document.getElementById("short-url").innerText.trim();
        navigator.clipboard.writeText(urlText).then(() => {
          alert("URL copied to clipboard!");
        });
      }
    </script>
  </body>
</html>
